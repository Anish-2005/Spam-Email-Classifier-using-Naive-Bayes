# Frontend

Next.js frontend (TypeScript + Tailwind) for the Spam Classifier.

Quick start:

```bash
cd frontend
npm install
npm run dev
```

The UI will call the backend at `NEXT_PUBLIC_API_URL` or default to:
`https://spam-email-classifier-using-naive-bayes-1.onrender.com`.
